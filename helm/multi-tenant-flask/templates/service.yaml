apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-tenant-flask.fullname" . }}
  labels:
    {{- include "multi-tenant-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-tenant-flask.selectorLabels" . | nindent 4 }}
